var a = prompt('Enter base'),
	 h = prompt('Enter height'),
	 triangleArea = a*h/2;

console.log('Triangle field with base '+ a +' and height ' + h +' equals ' + triangleArea);

alert('Triangle field with base '+ a +' and height ' + h +' equals ' + triangleArea);